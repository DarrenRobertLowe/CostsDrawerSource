/// setup_clickTimer();

doubleClickTime  = global.doubleClickTime;  // this should be loaded from preferences.
clickTime        = 0;                       // used to compare the current second against the first click

