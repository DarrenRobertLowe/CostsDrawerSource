/// is_punctuation(string)

var str = argument0;

if (str == " ")
//or (str = "'")
or (str == "-")
or (str == ".")
or (str == ",")
or (str == ")")
or (str == "(")
or (str == "[")
or (str == "]")
or (str == "-")
or (str == "!")
or (str == "?")
  then return true;
  else return false;
